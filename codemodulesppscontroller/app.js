// Angular module

// Params (Application name, Array of dependencies)
var myApp = angular.module('myApplication', []);

// Tell Angular, what part of the view does this app live in
// We are trying to connect the controller to the view

// Controller for view
// This is the model, the html invocation via div ng-controller
// is the view
myApp.controller('mainController', function() {
    
});
